version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: revenue_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: revenue_ai
    ports:
      - "5432:5432"